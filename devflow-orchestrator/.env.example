# DevFlow Orchestrator Environment Configuration
#
# NOTE: This is a template file. The actual environment configuration
# should be placed in the root directory: ../ecosystem-central-command/.env
#
# This follows the centralized configuration pattern where all ecosystem
# components share the same environment variables from the root .env file.
#
# Configuration Loading Order:
# 1. ../ecosystem-central-command/.env           (Primary - centralized config)
# 2. ./devflow-orchestrator/.env                 (Override - local development)
# 3. ../ecosystem-central-command/.env.local     (Override - centralized local)
# 4. ./devflow-orchestrator/.env.local          (Override - local development)

# GitHub Integration (Required)
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# Optional Configuration
NODE_ENV=development
LOG_LEVEL=info

# Cache Settings
CACHE_TTL=300
CACHE_CHECK_PERIOD=60

# Project Creation Settings
PROJECT_CREATION_DELAY=2000
AUTO_LINK_REPOSITORIES=false
DEFAULT_PROJECT_VISIBILITY=private